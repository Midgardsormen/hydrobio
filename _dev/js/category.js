const categoryHeader = document.getElementById('js-product-list-header');
const breadcrumbElement = document.getElementById('breadcrumbElement');

breadcrumbElement.insertAdjacentElement('afterend', categoryHeader);